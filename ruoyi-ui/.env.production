# 页面标题
VITE_APP_TITLE = Default API

# 生产环境配置
VITE_APP_ENV = 'production'

# Default API/生产环境
VITE_APP_BASE_API = '/prod-api'

# 是否在打包时开启压缩，支持 gzip 和 brotli
# 页面由宿主机 Caddy 托管并在传输层即时压缩，预压缩产物不会被 Caddy 自动使用，故关闭
VITE_BUILD_COMPRESS =