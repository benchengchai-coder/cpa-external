<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.pay.mapper.AiPayOrderMapper">

    <resultMap type="AiPayOrder" id="AiPayOrderResult">
        <id     property="orderId"      column="order_id" />
        <result property="outTradeNo"   column="out_trade_no" />
        <result property="userId"       column="user_id" />
        <result property="username"     column="username" />
        <result property="totalAmount"  column="total_amount" />
        <result property="subject"      column="subject" />
        <result property="tradeNo"      column="trade_no" />
        <result property="status"       column="status" />
        <result property="payTime"      column="pay_time" />
        <result property="notifyTime"   column="notify_time" />
        <result property="createBy"     column="create_by" />
        <result property="createTime"   column="create_time" />
        <result property="updateBy"     column="update_by" />
        <result property="updateTime"   column="update_time" />
        <result property="remark"       column="remark" />
    </resultMap>

    <sql id="selectAiPayOrderVo">
        select order_id, out_trade_no, user_id, username, total_amount, subject, trade_no, status,
               pay_time, notify_time, create_by, create_time, update_by, update_time, remark
        from ai_pay_order
    </sql>

    <select id="selectAiPayOrderById" parameterType="Long" resultMap="AiPayOrderResult">
        <include refid="selectAiPayOrderVo"/>
        where order_id = #{orderId}
    </select>

    <select id="selectAiPayOrderByOutTradeNo" parameterType="String" resultMap="AiPayOrderResult">
        <include refid="selectAiPayOrderVo"/>
        where out_trade_no = #{outTradeNo}
    </select>

    <select id="selectAiPayOrderList" parameterType="AiPayOrder" resultMap="AiPayOrderResult">
        <include refid="selectAiPayOrderVo"/>
        <where>
            <if test="userId != null">and user_id = #{userId}</if>
            <if test="outTradeNo != null and outTradeNo != ''">and out_trade_no = #{outTradeNo}</if>
            <if test="status != null and status != ''">and status = #{status}</if>
            <if test="params.beginTime != null and params.beginTime != ''">
                and date_format(create_time,'%Y%m%d') &gt;= date_format(#{params.beginTime},'%Y%m%d')
            </if>
            <if test="params.endTime != null and params.endTime != ''">
                and date_format(create_time,'%Y%m%d') &lt;= date_format(#{params.endTime},'%Y%m%d')
            </if>
        </where>
        order by order_id desc
    </select>

    <select id="selectLatestPendingOrderByUserId" resultMap="AiPayOrderResult">
        <include refid="selectAiPayOrderVo"/>
        where user_id = #{userId}
          and status = '0'
        order by create_time desc, order_id desc
        limit 1
    </select>

    <select id="selectPendingOrdersBeforeTime" resultMap="AiPayOrderResult">
        <include refid="selectAiPayOrderVo"/>
        where status = '0'
          and create_time &lt; #{beforeTime}
        order by create_time asc
        limit 200
    </select>

    <insert id="insertAiPayOrder" parameterType="AiPayOrder" useGeneratedKeys="true" keyProperty="orderId">
        insert into ai_pay_order
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="outTradeNo != null and outTradeNo != ''">out_trade_no,</if>
            <if test="userId != null">user_id,</if>
            <if test="username != null and username != ''">username,</if>
            <if test="totalAmount != null">total_amount,</if>
            <if test="subject != null and subject != ''">subject,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            <if test="remark != null and remark != ''">remark,</if>
            create_time
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="outTradeNo != null and outTradeNo != ''">#{outTradeNo},</if>
            <if test="userId != null">#{userId},</if>
            <if test="username != null and username != ''">#{username},</if>
            <if test="totalAmount != null">#{totalAmount},</if>
            <if test="subject != null and subject != ''">#{subject},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            <if test="remark != null and remark != ''">#{remark},</if>
            sysdate()
        </trim>
    </insert>

    <update id="updateOrderStatusCAS">
        update ai_pay_order
        set status     = #{toStatus},
            trade_no   = #{tradeNo},
            <if test="payTime != null">pay_time    = #{payTime},</if>
            update_time = sysdate()
        where order_id = #{orderId}
          and status   = #{fromStatus}
    </update>

    <update id="updateNotifyTime">
        update ai_pay_order
        set notify_time = #{notifyTime},
            update_time = sysdate()
        where order_id = #{orderId}
    </update>

</mapper>
